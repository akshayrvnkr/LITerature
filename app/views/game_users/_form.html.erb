<%= form_with(model: game_user, local: true) do |form| %>
  <% if game_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_user.errors.count, "error") %> prohibited this game_user from being saved:</h2>

      <ul>
      <% game_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :cards %>
    <%= form.text_area :cards %>
  </div>

  <div class="field">
    <%= form.label :last_online %>
    <%= form.datetime_select :last_online %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
